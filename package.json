{
  "name": "smithue-cli",
  "version": "0.15.1",
  "type": "module",
  "main": "dist/cli.js",
  "scripts": {
    "prebuild": "node scripts/clean-dist.cjs",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -p tsconfig.build.json --watch"
  },
  "files": [
    "dist/",
    "schemas/",
    "skill/",
    "package.json",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "commander": "^15.0.0"
  },
  "bin": {
    "smithue-cli": "dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "description": "CLI tool for controlling Unreal Engine editor via SmithUE plugin",
  "repository": {
    "type": "git",
    "url": "https://github.com/s2272756972-prog/smithue-cli.git"
  },
  "homepage": "https://github.com/s2272756972-prog/smithue-cli/tree/ue5.1-ue5.5-compat#readme",
  "bugs": {
    "url": "https://github.com/s2272756972-prog/smithue-cli/issues"
  }
}
